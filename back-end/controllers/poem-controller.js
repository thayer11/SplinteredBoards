var Card = require('../models/poem');
//At the bottom, you’ll have to fill out an export, 
//with all the function names you made in your controller, like the following (but not commented)
module.exports = {
  getAll: getAll,
};
